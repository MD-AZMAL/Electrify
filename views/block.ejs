<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
    <script src="main.js"></script>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">

    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

    <!-- Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        body {
            background: #111;
        }

        p {
            font-family: sans-serif;
            width: 100%;
            background: #d2f2d2;
            margin: 0;
            padding: 0.5rem;
        }

        p:nth-of-type(2n) {
            background: #f2d2d2;
        }

        .boldIt {
            font-weight: bolder;
            font-size: 1.2rem;
        }

        .card {
            margin: 0.5rem 0;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row">
            <% blockchain.forEach((block)=>{ %>
                <div class="col-sm-3">
                    <div class="card">
                        <div class="card-body">
                            <p>
                                <span class="boldIt">Index:</span>
                                <%= block.index %>
                            </p>
                            <p>
                                <span class="boldIt">prevHash:</span>
                                <%= block.prevhash %>
                            </p>
                            <p>
                                <span class="boldIt">Hash:</span>
                                <%= block.hash %>
                            </p>
                            <p>
                                <span class="boldIt">Nonce:</span>
                                <%= block.nonce %>
                            </p>
                            <p>
                                <span class="boldIt">SenderKey:</span>
                                <%= block.data.senderkey %>
                            </p>
                            <p>
                                <span class="boldIt">ReceiverKey:</span>
                                <%= block.data.receiverkey %>
                            </p>
                            <p>
                                <span class="boldIt">Power:</span>
                                <%= block.data.power %>
                            </p>
                            <p>
                                <span class="boldIt">Money:</span>
                                <%= block.data.money %>
                            </p>
                        </div>
                    </div>
                </div>
                <% }) %>
        </div>
    </div>
</body>

</html>